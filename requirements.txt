# The dependency list now lives in pyproject.toml (see `[project.dependencies]`).
# This file is kept so that `pip install -r requirements.txt` keeps working;
# it installs Matcha-TTS in editable mode together with all of its dependencies.
-e .
